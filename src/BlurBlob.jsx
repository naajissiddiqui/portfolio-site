import React from "react";

const BlurBlob = () => {
  return (
    <div
      className="absolute "
      style={{
        top: top,
        left: left,
        width: width,
        height: height,
        transform: "translate(-50%, -50%)",
      }}
    ></div>
  );
};

export default BlurBlob;
